# Ignore node_modules in both frontend and backend
frontend/node_modules
backend/node_modules

# Ignore .next build folder (Next.js)
frontend/.next
backend/dist

# Optional: Ignore environment variables
frontend/.env
backend/.env

